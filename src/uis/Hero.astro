---
import { Image } from "astro:assets";
import phone from "@assets/phones.png";
import Btn from "@uis/components/btn.astro";

const hero = {
  title: "Reduce tu factura eléctrica con energía solar",
  subtitle:
    "Únete a miles de colombianos que ya generan su propia energía limpia. Calcula, diseña e instala tu sistema solar desde tu celular.",
  primary_action: "Descargar App Gratis",
  secondary_action: "Involúcrate",
  items: [
    {
      icon: "📱",
      text: "100% Gratis",
    },
    {
      icon: "🇨🇴",
      text: "Hecho en Colombia",
    },
    {
      icon: "💰",
      text: "Ahorra hasta 70%",
    },
  ],
};

const { title, subtitle, primary_action, secondary_action, items } = hero;
---

<section id="inicio" class="bg-block-dark">
  <div class="container mx-auto px-6">
    <div class="flex flex-col items-center lg:flex-row">
      <div class="mb-8 lg:mb-0 lg:w-1/2 lg:pr-12">
        <h1 class="text-hero text-primary-dark mb-6 leading-tight font-bold">
          {title}
        </h1>
        <p class="text-inline-dark mb-8 text-xl leading-relaxed">
          {subtitle}
        </p>

        <div
          class="mb-8 flex flex-col space-y-4 sm:flex-row sm:space-y-0 sm:space-x-4"
        >
          <Btn label={primary_action} size="lg" />
          <Btn label={secondary_action} type="outline" size="lg" />
        </div>

        <div
          class="text-inline-dark flex flex-wrap items-center space-x-6 text-sm"
        >
          {
            items.map(({ icon, text }) => {
              return (
                <div class="flex items-center space-x-2">
                  <span class="text-2xl">{icon}</span>
                  <span class="font-medium">{text}</span>
                </div>
              );
            })
          }
        </div>
      </div>

      <div class="flex justify-end lg:w-1/2">
        <Image
          src={phone}
          alt="A bird sitting on a nest of eggs."
          class="h-screen w-5/7"
        />
      </div>
    </div>
  </div>
</section>
